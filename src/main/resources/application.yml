spring:
  application:
    name: lowcode-workflow

  # PostgreSQL Database
  datasource:
    url: jdbc:p6spy:postgresql://ep-frosty-bush-a5f8yx09-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    username: neondb_owner
    password: npg_iW3juwZGAI8K

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

# SpringDoc / Swagger UI
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  paths-to-exclude:
    - /profile
    - /profile/**
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

# Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  tracing:
    enabled: true
    sampling:
      probability: 1.0
  endpoint:
    health:
      show-details: when-authorized

logging:
  # Put trace/span into each log line.
  pattern:
    level: "%5p [${spring.application.name:-},%X{traceId:-},%X{spanId:-}]"
  level:
    # Logbook prints request/response only when this logger is TRACE.
    org.zalando.logbook: TRACE
    p6spy: INFO
    com.p6spy: INFO

logbook:
  format:
    style: json
  exclude:
    - /actuator/**
    - /health
    - /error
    - /v3/api-docs/**
    - /swagger-ui/**
    - /swagger-ui.html
  obfuscate:
    headers:
      - Authorization
      - Cookie
      - Set-Cookie
    parameters:
      - password
      - token
